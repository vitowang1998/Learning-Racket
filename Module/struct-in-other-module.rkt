#lang racket
;; data-structure created from another module is not accessable from this module
;; Even though we defined a data-structure with the same name, the built-in functions
;; wouldn't work for the struct defined in that module
